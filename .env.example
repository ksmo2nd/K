# KSWiFi App Environment Configuration
# Copy this file to .env and fill in your real values

# ========================================
# FRONTEND ENVIRONMENT VARIABLES
# ========================================
# Set these in your Vercel dashboard for frontend deployment

NEXT_PUBLIC_BACKEND_URL=https://your-backend-url.render.com
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ========================================
# BACKEND ENVIRONMENT VARIABLES
# ========================================
# Set these in your Render dashboard for backend deployment

# Supabase Configuration
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-service-role-key-here
SUPABASE_ANON_KEY=your-anon-key-here

# Database (Same as Supabase PostgreSQL)
DATABASE_URL=postgresql://postgres:your-password@db.your-project-id.supabase.co:5432/postgres

# Security
SECRET_KEY=your-32-character-secret-key-generate-with-openssl-rand-hex-32
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Server Configuration (optional - has defaults)
HOST=0.0.0.0
PORT=8000
DEBUG=false

# CORS Origins (update with your domains)
ALLOWED_ORIGINS=["https://your-frontend.vercel.app","https://kswifi.com"]

# Redis (optional - defaults to localhost)
REDIS_URL=redis://localhost:6379

# eSIM Provider (now optional with self-generated eSIMs)
# You can use mock values since we generate our own eSIMs
ESIM_PROVIDER_API_URL=https://api.mock-esim-provider.com
ESIM_PROVIDER_API_KEY=mock-api-key
ESIM_PROVIDER_USERNAME=mock-username
ESIM_PROVIDER_PASSWORD=mock-password

# Data Monitoring (optional - has defaults)
DATA_CHECK_INTERVAL_MINUTES=5
LOW_DATA_THRESHOLD_MB=100.0

# Monitoring (optional)
SENTRY_DSN=your-sentry-dsn-for-error-tracking
LOG_LEVEL=INFO

# ========================================
# HOW TO GET THESE VALUES
# ========================================

# SUPABASE VALUES:
# 1. Go to https://supabase.com/dashboard
# 2. Select your project
# 3. Settings → API
# 4. Copy:
#    - Project URL → SUPABASE_URL
#    - anon public key → SUPABASE_ANON_KEY  
#    - service_role key → SUPABASE_KEY

# DATABASE_URL:
# 1. Supabase Dashboard → Settings → Database
# 2. Copy connection string
# 3. Replace [YOUR-PASSWORD] with your actual database password

# SECRET_KEY:
# Generate with: openssl rand -hex 32

# BACKEND_URL:
# Your deployed FastAPI backend URL (Render, Railway, etc.)